// tslint:disable-next-line:no-empty-interface
export interface JwtResponseInterface {
  dataUser: {
    id: number;
    name: string;
    email: string;
    accessToken: string;
    expierIn: string;
  };
}
